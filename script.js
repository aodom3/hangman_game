// words being used
let words = ["vanessa","pizza","tacos","golden girls","unicorn","chimichanga"];
// random word chosen from group of words
let secretWord = words[Math.floor(Math.random()*words.length];
// blank "_" that is shown on the screen to be populated
let ansArray = (for i=0; i < words.length; i++) {
    ansArray[] = "_"
}
let remainingGuesses < 6;

